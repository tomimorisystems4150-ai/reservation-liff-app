{
  "name": "reservation-onboarding-worker",
  "version": "1.0.0",
  "description": "LINE予約システム 自動オンボーディングサーバー (Cloudflare Workers)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
