# システム受け入れテスト チェックリスト

### フェーズ1：システムの骨格と設定機能
| ID | テスト対象 | テスト内容 | 期待される結果 | 結果 |
| :--- | :--- | :--- | :--- | :--- |
| 1-1 | DB（シート） | スプレッドシートを開く。 | `Config`シートが存在し、`shopName`, `serviceMenus`, `isStaffFeatureEnabled`, `staffs`, **`maxBulkBookings`** など、これまでに設定したキーがすべて存在するか？ | |
| 1-2 | カスタムメニュー | スプレッドシートを再読み込み（リロード）する。 | 画面上部に「▼ システム管理」というメニューが表示されるか？ <br> その中の「設定画面を開く」をクリックすると、設定画面のURLがポップアップで表示されるか？ | |
| 1-3 | 設定画面（初期表示） | 設定画面のURLを開く。 | `Config`シートに設定されている値（店舗名、営業時間、メニュー、担当者機能ON/OFF、**一括予約上限数**など）が、画面の各フォームに正しく表示されているか？ | |
| 1-4 | 設定画面（保存・反映） | 店舗名やメニューの内容などを変更し、「設定を保存」ボタンを押す。 | 「設定を保存しました。」と表示され、`Config`シートの値が変更後の内容に更新されているか？ | |
| 1-5 | バリデーション（必須項目） | 店舗名、営業時間、LINE連携設定などの必須項目をいずれか空にして、保存ボタンを押す。 | 「有効な値を指定してください。」と表示され、該当の入力欄が赤枠になり、保存されないか？ | |
| 1-6 | バリデーション（予約単位） | 「予約の時間単位」に`10`（15未満）、`16`（5の倍数でない）、`65`（60より大きい）などの無効な値を入力して、保存ボタンを押す。 | エラーメッセージが表示され、入力欄が赤枠になり、保存されないか？ | |
| 1-7 | バリデーション（時間整合性） | 営業時間の開始を「21:00」、終了を「20:00」のように逆転させて、保存ボタンを押す。 | エラーメッセージが表示され、両方の時間入力欄が赤枠になり、保存されないか？ | |
| 1-8 | バリデーション（メニュー重複） | 同じメニュー名（例: 「カット」）を2行入力して、保存ボタンを押す。 | エラーメッセージが表示され、重複している両方のメニュー名入力欄が赤枠になり、保存されないか？ | |
| 1-9 | バリデーション（担当者重複） | 担当者機能を「利用する」にし、同じ担当者名または同じGmailアドレスを2行入力して、保存ボタンを押す。 | エラーメッセージが表示され、重複している両方の入力欄が赤枠になり、保存されないか？ | |
| 1-10 | 認証（正常系） | `Config`シートの`adminEmail`に登録されているGoogleアカウントで設定画面にアクセスする。 | 設定画面が正常に表示されるか？ | |
| 1-11 | 認証（異常系） | `Config`シートに登録されていない、別のGoogleアカウントで設定画面のURLにアクセスする。 | 「アクセス権がありません」という画面が表示され、設定内容が見えないか？ | |
| 1-12 | バリデーション（一括予約上限数） | 設定画面の「一括予約上限数（`maxBulkBookings`）」に`0`（1未満）や`-1`（負数）、小数点を含む値などの無効な値を入力して、保存ボタンを押す。 | エラーメッセージが表示され、入力欄が赤枠になり、保存されないか？正常値（例: `3`）では保存が成功し、`Config`シートに反映されるか？ | |

### フェーズ2：予約コアロジックとエンドユーザー体験
**【前提条件】フロントエンドのコードがGitHub Pagesにデプロイされ、そのURLがLINE DevelopersのLIFFエンドポイントに正しく設定されていること。**

| ID | テスト対象 | テスト内容 | 期待される結果 | 結果 |
| :--- | :--- | :--- | :--- | :--- |
| 2-0-A | Webhook接続 | LINE公式アカウントのトーク画面で、「予約確認」以外の任意のテキスト（例:「こんにちは」）を送信する。 | 何も応答がないこと。成功すれば、メッセージがGASに届いているが、処理対象外として無視されていることを示す。 | |
| 2-0-B | LIFF接続 | LINE公式アカウントのリッチメニュー等に設定した、予約画面を開くためのボタンをタップする。 | LIFFアプリが起動し、**GitHub Pagesでホストされた予約画面**が表示されること。成功すれば、LIFF ID、エンドポイントURL、GitHub Pagesのデプロイが正常に動作していることを示す。 | |
| 2-0-C | LIFF初期化 | LIFF画面起動後、ローディングが終了し予約フォームが表示される。 | **GitHub Pages環境で`liff.init()`および`liff.getProfile()`が成功し、LINEユーザー情報が取得できていること。UIが操作可能になること。** | |
| 2-1 | LIFF空き枠 | **メニュー（例: 60分）を選択する。**ブロックカレンダーに「13:00-14:00 休憩」という予定を入れる。LIFF画面を開く。 | **12:30、13:00、13:30の枠が予約不可になっているか？（60分のメニューが収まらないため）** <br>**【未実装】このテストはフェーズ3完了後に実施。** | |
| 2-2 | LIFF空き枠 | （担当者機能ONの場合）担当者を切り替える。 | 担当者ごとに予約不可の時間帯（シフト休みなど）が正しくカレンダーに反映されるか？ <br>**【未実装】このテストはフェーズ3完了後に実施。** | |
| 2-2-B | メニュー別空き枠 | **所要時間が短いメニュー（例: 30分）と長いメニュー（例: 90分）を切り替える。** | **90分のメニューを選択した場合、連続90分確保できない時間帯は予約不可になるが、30分のメニューに切り替えると予約可能になるなど、動的に表示が切り替わるか？** | |
| 2-2-C | 複数日時選択（基本） | `Config`の`maxBulkBookings`を`3`に設定する。LIFF画面でメニューを選択後、タイムテーブルから空き枠を3件選択する。 | **1件目・2件目・3件目の選択が順に受け付けられ、選択済み枠がハイライト表示されるか？選択件数カウンター（例:「3/3 選択済み」）が正しく更新されるか？「まとめて予約確定」ボタンが活性化されるか？** | |
| 2-2-D | 上限制御（グレーアウト） | テスト2-2-Cに続けて、すでに3件選択された状態でさらに別の空き枠をタップする。 | **上限（3件）に達した時点で、未選択の空き枠がグレーアウト（選択不可）になっているか？上限到達後のタップは無視され、選択件数が4件に増えないか？** | |
| 2-2-E | 選択解除と再選択 | テスト2-2-Dの状態で、選択済みの枠のうち1件をタップして選択解除する。 | **選択解除により件数が2件になり、グレーアウトしていた枠が再び選択可能（◯表示）に戻るか？解除した枠を再度タップすると選択され3件に戻るか？** | |
| 2-3 | LIFF予約フロー | 初回ユーザーとして、空き枠から日時を選択し予約を確定させる。 | 顧客情報登録画面に遷移し、登録後に予約が完了するか？完了後、予約シートと顧客マスタにデータが作成されるか？ <br>**【未実装】顧客登録機能は未実装のため、このテストは実施不可。テスト2-4で代替する。** | |
| 2-4 | LIFF予約フロー（単件） | LIFFのテストフォームで**任意のメニューを選択し、**未来の日時を1件選択して「この内容で予約する」ボタンを押す。 | **フロント(GitHub Pages)からバックエンド(GAS)へ、選択したメニュー情報を含む予約データがfetch API経由で正しく送信されること。**予約シートに新しい行が1件追加され、Googleカレンダーに**選択したメニューの所要時間で**予定が1件作成され、LINEに「ご予約ありがとうございます！」というメッセージが届き、LIFF画面が閉じること。 | |
| 2-4-B | LIFF予約フロー（複数日時一括） | `maxBulkBookings`を`3`に設定した状態で、メニューを選択後に異なる日の空き枠を3件選択し、「まとめて予約確定」ボタンを押す。 | **予約シートに3行が追加されるか？Googleカレンダーに3件の個別イベントが正しい日時・所要時間で作成されるか？LINEに「〇件のご予約を承りました」等の通知が届くか？予約完了画面に3件分の予約情報が表示されるか？** | |
| 2-5 | カレンダー登録 | テスト2-4の予約完了時に表示されるGoogleカレンダーURLをクリックする。 | 予約した日時・内容が正しく反映されたGoogleカレンダーの予定作成画面が、新しいタブで開くか？ <br>**※現時点では、予約完了後にalertダイアログでURLが表示される。そのURLが正しく機能するかを確認する。** | |
| 2-6 | カレンダー登録 | 予約完了画面で「その他カレンダーに追加」ボタンを押す。 | 予約情報を含む.icsファイルがダウンロードされるか？そのファイルをPCやスマホで開くと、カレンダーアプリに正しく予定がインポートされるか？ <br>**【未実装】ICSファイル生成機能は未実装のため、このテストはフェーズ4で実施する。** | |
| 2-7 | LIFF予約フロー（同時実行・単件） | （同時実行テスト）2台のスマホで同時に同じ枠を予約しようとする。 | 先に「確定」を押した方が予約成功し、もう一方は「タッチの差で埋まりました」等のエラーメッセージが表示されるか？ <br>**【未実装】最終空き枠チェック機能が未実装のため、このテストは実施不可。現時点ではダブルブッキングが発生してしまう。** | |
| 2-7-B | LIFF予約フロー（一括確定時の競合） | `maxBulkBookings`を`3`に設定し、3件の枠を選択して確定しようとする直前に、別ユーザーが選択枠のうち1件を先に予約する。 | **先に予約した別ユーザーの予約は成功し、後から確定しようとした3件はすべてキャンセルされ（部分確定しない）、「〇月〇日〇:〇〇の枠が埋まってしまいました。選択をやり直してください。」等のエラーメッセージが表示されるか？** | |
| 2-8 | LINE予約確認 | テスト2-4で予約を作成した後、LINEのトーク画面で「予約確認」と送信する。 | 「今後のご予約一覧です。」というメッセージと共に、先ほど予約した日時が正しいフォーマットで返信されるか？成功すれば、Webhook経由でのデータ読み取りと返信機能が正常に動作していることを示す。 | |
| 2-9 | LINE予約確認 | （予約が1件もない状態で）LINEのトーク画面で「予約確認」と送信する。 | 「現在、ご予約はありません。」というメッセージが返信されるか？ <br>**※予約シートが空、または未来の予約がない状態でテストを実施する。** | |

### フェーズ3：店舗向け管理機能とバックグラウンド処理
| ID | テスト対象 | テスト内容 | 期待される結果 | 結果 |
| :--- | :--- | :--- | :--- | :--- |
| 3-1 | Web画面認証 | 担当者リストに登録されていないGoogleアカウントで管理Web画面にアクセス | 「アクセス権がありません」等のエラー画面が表示され、操作ができないか？ **(GASの`Session.getActiveUser()`による認証が機能すること)** | |
| 3-2 | Web画面機能 | 予約イベントのURLから来店処理画面を開き、「来店」ボタンを押す | 予約シートのステータスが「来店」に更新され、Googleカレンダーのイベント色が緑色に変わるか？ | |
| 3-3 | Web画面機能 | 同様に「無断キャンセル」ボタンを押し、動作を確認する | 予約シートのステータスが「無断キャンセル」に更新され、カレンダーのイベント色が赤色に変わるか？ | |
| 3-4 | Web画面機能 | 終業後バッチで作成されたURLから未確定一覧画面を開く | 当日の未確定予約のみが表示され、「一括来店処理」ボタンでステータスが一括更新されるか？ | |
| 3-5 | カレンダー同期 | ブロックカレンダーの予定を削除する | LIFF画面で、その時間帯が予約可能枠として復活しているか？ | |
| 3-6 | 夜間バッチ | （手動で）満席タイムテーブル作成バッチを実行する | **予約シートの予約状況に基づき、最小予約単位（例：15分や30分間隔）ごとに計算が行われ、満席となっている時間帯が『満席タイムテーブル』シートに正しく記録されるか？** | |

### フェーズ4：運用・自動化機能と最終調整
| ID | テスト対象 | テスト内容 | 期待される結果 | 結果 |
| :--- | :--- | :--- | :--- | :--- |
| 4-1 | シナリオテスト（単件） | **メニューを選択して**新規予約（1件）→予約変更→リマインド受信→来店処理→顧客データ確認 | 一連の顧客体験フローを通して、すべてのデータ（予約、顧客、カレンダー）が正しく更新され、通知も適切なタイミングで届くか？ | |
| 4-1-B | シナリオテスト（複数日時一括） | `maxBulkBookings`を`3`に設定し、**3件の日時を一括選択して**予約→それぞれの日時に来店処理→顧客データ確認 | 3件分すべてのカレンダーイベント・予約シートレコードが正しく更新され、各来店処理が独立して行えるか？ | |
| 4-2 | リマインダー | （時間を操作して）予約をキャンセルする | キャンセルした予約のリマインド通知が送信されないことを確認できるか？ | |
| 4-3 | 管理者引き継ぎ | 後任者のGoogleアカウントでログインし、Web画面から引き継ぎ処理を実行 | Configシートの管理者が後任者に変更され、システムが問題なく動作し続けるか？古いトリガーが無効化されているか？ | |
| 4-4 | 半自動更新 | カスタムメニューからシステム更新を実行する | GASプロジェクトのライブラリ参照バージョンが、指定の新しいバージョンに更新されているか？ | |
| 4-5 | エラーログ | （意図的にエラーを発生させ）動作を確認する | エラーログシートにエラー内容が記録され、Web画面でその内容を確認できるか？ | |
| 4-6 | 堅牢性テスト | 各Web画面の入力フォームに、想定外の文字列（絵文字、記号、長文など）を入力して保存する | システムがクラッシュせず、適切に処理（またはエラー表示）されるか？ | |

### フェーズ5：初期導入の完全自動化（SaaS化対応）
**【前提条件】Cloudflare Workers上にオンボーディングサーバーがデプロイされ、Google Cloud ConsoleでのOAuthクライアント設定が完了していること。**

| ID | テスト対象 | テスト内容 | 期待される結果 | 結果 |
| :--- | :--- | :--- | :--- | :--- |
| 5-1 | オンボーディングフロー（正常系） | オンボーディングページにアクセスし、「Googleアカウントで開始」ボタンを押してGoogleアカウントでログインし、要求される権限（スプレッドシート・カレンダー等）をすべて承認する。 | エラーなくオンボーディングが完了し、「導入が完了しました」等の画面が表示されるか？バックグラウンドでスプレッドシートが自動作成・初期化され、GASがデプロイされているか？ | |
| 5-2 | 自動初期化（シート構造） | テスト5-1完了後、自動作成されたスプレッドシートを開く。 | `Config`シート・`予約`シート・`満席タイムテーブル`シートが自動生成され、各シートに正しいヘッダー行が設定されているか？`Config`シートの`maxBulkBookings`を含む主要キーに初期値が設定されているか？ | |
| 5-3 | エラーハンドリング（権限不足） | オンボーディング中にGoogleの権限承認ダイアログで「キャンセル」または権限の一部を拒否する。 | オンボーディングが中断され、「権限の承認が必要です」等の適切なエラーメッセージが表示されるか？スプレッドシートやGASが中途半端な状態で残らないか？ | |
| 5-4 | エラーハンドリング（途中離脱） | オンボーディング処理の途中でブラウザを閉じるなどして処理を中断する。 | 再度オンボーディングページにアクセスすると、最初からやり直せるか？前回の中断による不整合データ（未完成のスプレッドシート等）が残っていても、上書き再実行で正常にセットアップが完了するか？ | |
